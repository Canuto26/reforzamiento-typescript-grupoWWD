// Declaracion de las variables necesarias.
const nombre: string = "Danna";
const carrera: string = "Ingeniería de Sistemas";
const semestre: number = 9;

// Usamos una plantilla de cadena (template string) para construir el mensaje.
// usando el formato `${variable}` y se escriben con comillas invertidas (`).
const mensaje: string = `Hola, mi nombre es ${nombre}, estudio ${carrera} y actualmente estoy en el semestre ${semestre}.`;

// Mostramos el mensaje en la consola. hola
console.log(mensaje);